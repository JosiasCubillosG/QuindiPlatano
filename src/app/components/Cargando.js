import React from 'react'
import './styles/cargando.css'

const Cargando = () => {
    return(
        <div className="cargando">
            <div className="cargando__animacion">Cargando....</div>
        </div>
    )
}

export default Cargando;